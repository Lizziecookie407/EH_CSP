// EH 6th Functions Notes
/*
What a function is:
 strage contaners for actions to use over and over
Why we use functions
 to reuse actions and to make coding easier
What are arguments and parameters?
 peramiters are what goes in the parenthases when defining the function, arguements go when calling the function
How do arguments and parameters work together?
 arguments go in the parameters places
What are return statements?
 what is saved after the function
How do return statements change how you define a function in C?
 must state what type of data you are returning
What do return statements do with the information?
 save it to be used
*/

#include <stdio.h>

//do not write funtions inside of other funtions
//first word is the data type of the return. no return = void. no perameters = void
void birthday(char* name, int age){    
    printf("Happy Birthday to you\n");
    printf("Happy Birthday to you\n");
    printf("Happy Birthday dear %s\n", name);
    printf("Happy Birthday, %s is now %d\n", name, age);
}

int add(int num1, int num2){
    return num1+num2;
}

char* get_name(void){
    char *name;
    printf("What is your name: ");
    scanf("%s", name);
    return *name;
}

float get_num(void){
    float num;
    printf("Enter a number: ");
    scanf("%d", &num);
    return num;
}

int main(void){

    birthday("Eliza", 15);
    birthday("Juno", 2);
    birthday("Luna", 4);

    char *user = get_name();
    birthday(user, 15);

    int addition = add(5,4);
    printf("%d\n", addition);

    float our_number = get_num();
    printf("%f", our_number);

    
    return 0;
}